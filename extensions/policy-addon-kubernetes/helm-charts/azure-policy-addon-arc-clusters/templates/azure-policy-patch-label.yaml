# 
# The job in this YAML file adds the 'control-plane' label to 'kube-system' and 'azure-arc' namespaces.
# The job is executed in helm pre-install and pre-upgrade cycle.
#
# For namespaces with 'control-plane' label...
#     1. Kubernetes admission requests with violations are not denied by Gatekeeper.
#     2. Auditing results are still available.
#
apiVersion: v1
kind: ServiceAccount
metadata:
  name: azure-policy-patch-label-account
  namespace: kube-system
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "20"
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: azure-policy-patch-label-cluster-role
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "20"
rules:
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["get", "patch"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: azure-policy-patch-label-role-binding
  namespace: kube-system
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "20"
subjects:
- kind: ServiceAccount
  name: azure-policy-patch-label-account
  namespace: kube-system
roleRef:
  kind: ClusterRole
  name: azure-policy-patch-label-cluster-role
  apiGroup: rbac.authorization.k8s.io
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: azure-policy-patch-label-role-binding
  namespace: azure-arc
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "20"
subjects:
- kind: ServiceAccount
  name: azure-policy-patch-label-account
  namespace: kube-system
roleRef:
  kind: ClusterRole
  name: azure-policy-patch-label-cluster-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: Job
metadata:
  name: azure-policy-patch-label
  namespace: kube-system
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    metadata:
      labels:
        app: azure-policy-patch-label
    spec:
      serviceAccountName: azure-policy-patch-label-account
      automountServiceAccountToken: true
      restartPolicy: Never
      terminationGracePeriodSeconds: 0
      containers:
        - name: azure-policy-patch-label
          image: {{ .Values.alpine.image.name }}:{{ .Values.alpine.image.tag }}
          imagePullPolicy: IfNotPresent
          command: 
            - "/bin/sh"
            - "-ec"
            - |
              set -o pipefail
              token=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
              namespaces="kube-system azure-arc"
              for namespace in ${namespaces}
              do
                echo "Retrieving namespace ${namespace} details"
                namespaceDetails=$(curl -s -X GET -k https://kubernetes.default.svc/api/v1/namespaces/${namespace} \
                -H "Authorization: Bearer ${token}" \
                -H "Content-Type: application/json" \
                -H "Accept: application/json")
                kind=$(echo $namespaceDetails | jq .kind)
                if [ "$kind" = "\"Namespace\"" ]
                then
                  cplabel=$(echo $namespaceDetails | jq .metadata.labels.\"control-plane\")
                  if [ "$cplabel" = "null" ]
                  then
                    echo "Adding control-plane label to ${namespace}"
                    patchstatus=$(curl -s -X PATCH -k https://kubernetes.default.svc/api/v1/namespaces/${namespace} \
                    -d '{ "metadata": { "labels": { "control-plane": "controller-manager" } } }' \
                    -H "Authorization: Bearer ${token}" \
                    -H "Content-Type: application/strategic-merge-patch+json" \
                    -H "Accept: application/json")
                  else
                    echo "control-plane label already exists on namespace ${namespace}"
                  fi
                else
                  echo "Failed to retrieve ${namespace} details"
                fi
              done