{
    "properties": {
        "displayName": "Copy resource group tag to resource",
        "mode": "Indexed",
        "description": "Copy a named tag from the resource group tag to the resource.  For example, a tag named example on the resource group will be copied to the resources.  Change the word example in the policy rule to change the name of the tag copied to a resource.",
        "policyRule": {
            "if": {
                "field": "tags.example",
                "notEquals": "[resourceGroup().tags.example]"
            },
            "then": {
                "effect": "append",
                "details": [
                    {
                        "field": "tags.example",
                        "value": "[resourceGroup().tags.example]"
                    }
                ]
            }
        }
    }
}
